<div class="container">
  <div class="row">
      <div class="card">
        <div class="card-header">ジャンル一覧</div>
        <div class="card-body">
          <% genres.each do |genre| %>
            <p class="card-text offset-2"><%= genre.name %></p>
          <% end %>
        </div>
      </div>
  </div>
</div>